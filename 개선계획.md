# 외부 검토 기반 개선 계획 (2026-08-24)

데모 전체(파이프라인 순서, 계산식, 단가, 소요량, 세그멘테이션 렌더, UX 흐름)를
외부 AI 검토 2건에 이미지와 계산 추적을 첨부해 보내고, 웹 근거 리서치 2건을
받아 교차 검증했다. 원문은 세션 기록에 있고, 이 문서는 판정과 반영 결과다.

## 1. 즉시 반영한 것 (이번 커밋)

| 지적 | 출처 | 반영 |
|---|---|---|
| 완제품 질량 검증에 투입질량(수율 나눔 포함)을 씀 | 검토2 | 완제품=순질량(부피x밀도, 면적xGSM), 구매 투입은 별도 필드로 분리 |
| 포장재(티슈 18g)가 신발 무게에 포함 | 검토2 | 포장 조립군은 완제품 질량에서 제외하고 목록으로 표시 |
| 접착제 습량 전체를 무게에 넣음 | 검토2 | 건조 고형분 50%만 산입, 근거 명시 |
| Shoe Box 에 surface_region 역할 | 검토1 | fixed_quantity 역할 신설, 개수 품목은 지오메트리 불확실성 없음(C2 가능) |
| 면적 소재 14라인 질량 미산정 | 검토1·2 | 소재 스펙에 면밀도(GSM) 14건 추가, 질량 검사 완성 |
| 한 장 이미지의 좌우 팽창 미검증 | 검토1 | 폭/길이 비율 검사 신설. 실측 0.436 > 통상 0.42 로 실제 경고 발생 |
| 포장이 소재비의 31%인데 한 덩어리 표시 | 검토2 | 소재 소계를 어퍼/솔/접착·화학/포장/부자재로 분해 표시 |
| 소계가 총원가처럼 읽힘 | 검토2 | 카드 제목을 "가격 확정된 N/M라인의 소재·부자재 소계"로, 라인 커버리지와 전체 원가 커버리지를 분리 표기 |
| 기하 추정 세그멘테이션의 라벨 부재 | 사용자·검토1·2 | **사내 신발 파트 모델(h100)을 실제 실행해 통합** — 아래 3절 |
| 업로드 UX 불명확 | 사용자 | 드래그앤드롭 존 + 조건 칩 + 미리보기 + 해상도 경고 + 버튼 비활성 |
| 3D 검은 균열 선 | 사용자 | 열린 껍질의 뒷면 렌더 누락이 원인. 양면 렌더로 수정 (면적 계산은 서버 단면 기준 그대로 — 검토2가 경고한 2배 오류 없음) |
| 파랑 위 파랑 글자 | 사용자 | 색 면 위 글자는 전부 흰색 계열로 통일 |

질량 검사 결과 해석이 바뀐 것: 기하 세그멘테이션 프로젝트가 완제품 673g 대비
목표 600g 으로 **fail_over_estimate** 가 됐다. 폭 검사(too_wide)와 같은 원인
(한 장 이미지의 좌우 팽창)을 두 게이트가 독립적으로 잡는다.

## 2. 타당하지만 채택하지 않은 것

| 제안 | 출처 | 판단 |
|---|---|---|
| 노무·기계·금형을 업계 평균으로 채워 총액을 무조건 표시 | 검토1 | **거부.** 이 데모의 핵심 계약이 "입력 없으면 차단"이다. 임의 평균으로 채우면 앞선 검토가 지적한 부분원가 왜곡이 그대로 돌아온다. 시나리오 추정 모드는 별도 토글로 추후 검토 |
| 사이즈 라벨에 카테고리 상수를 곱해 외부 길이 자동 추정 | 검토1 | **부분 채택 예정.** 자동 확정은 안 하되, 입력란 옆 힌트("러닝화 260 라벨은 통상 285~300mm")로 제공 |
| 부피 과대분을 폭 비율로 자동 축소 | 검토1 | **거부.** 측정하지 않은 값을 고치는 것은 또 다른 조작. 경고와 등급 강등으로 처리 |
| 단가 DB를 면적당 단가로 재정규화 | 검토1·2 | 워크북 원본이 선형 m 단가라 당장은 유지. 유효 폭이 같은 소재 스펙 안에 있어 연동은 깨지지 않음. C2 전환 시 재정규화 |

## 3. 사내 세그멘테이션 모델 통합 (핵심)

h100 의 신발 파트 모델(28클래스, 신발 도메인 mAP 0.470)을 이번에 처음 실행했다.
지금까지 데모는 이 모델을 **쓰지 않고** 있었다.

- 환경: 의존성 충돌 1건 수리 후 설치 검증 통과 (렌더 라이브러리 버전 고정 순서 문제)
- 입력: 데모 원본 메시 140만면 → 28만면 프록시로 데시메이션 후 추론
- 출력: display 파트 42개 → 클래스 병합 8개
  (outsole 0.90 / quarter-fabric 0.97 / lining 0.87 / midsole 0.92 /
   laces 0.87 / tongue 0.84 / loop 0.85 / heeltap 0.67)
- face 커버리지 71.9%

기하 추정 대비 차이:
- **라이닝이 규칙 proxy 에서 실측이 됐다** (기하 추정은 라이닝을 볼 수 없다)
- 어퍼가 한 장(quarter-fabric)으로 잡힌다 — 원피스 메시 어퍼의 제조 실제와 일치
- 검토2가 의심한 "midsole/outsole 경계 혼선"이 모델 라벨로 해소된다

통합 방식: `tools/import_segmentation.py` 가 모델 출력(라벨 배열 + 클래스 JSON)을
클래스당 한 메시로 병합하고 canonical part 로 매핑해 프로젝트로 들인다.
파이프라인은 모델 매핑이 있으면 기하 추정 대신 그것을 쓴다.
데모에서 두 프로젝트를 나란히 볼 수 있다 (상단 프로젝트 선택):
- DEMO-RUN-001: 기하 세그멘테이션 (기존)
- DEMO-SEM-001: 사내 모델 세그멘테이션 (신규)

## 4. 리서치 근거 (다음 단계의 数値 목표)

- 어퍼 소재 소요량 모델의 실측 평균절대오차 9.6% (2024 공개 연구) —
  현재 상수 pattern_factor 방식의 개선 목표선
- 신발 가죽 재단 손실 문헌치 20~45% (평균 약 30%), 의류 marker 손실 10~15% —
  nesting_yield 가정값(0.76~0.85)의 근거 범위와 일치
- 소요량 보정 실무식: 전개면적+시접 여유 를 marker efficiency 로 나누고
  수축·결점·사이즈 믹스 계수를 곱함 — C2 전환 시 채택할 형태
- 세그멘테이션 후처리 관행: 작은 island 병합, 경계의 곡률·심라인 스냅,
  좌우 대칭 전파 — 사내 모델 파이프라인이 이미 대부분 수행

## 5. 다음 순서 (미착수)

1. 스케일 입력 힌트(사이즈 라벨 → 통상 외부 길이 범위)와 폭·힐스택 추가 실측 입력
2. 세그먼트 화면에 병합/분할/삭제/규칙 전환 액션 (검토2 제안)
3. 단계 탭 7→3 그룹 통합 (검토1 제안, 두 검토 공통)
4. 솔 부피의 실루엣 압출 추정 경로 (모델 세그멘테이션 프로젝트의 솔 질량 공백 해소)
5. 사이즈별 재계산과 Gold 스타일 축적
