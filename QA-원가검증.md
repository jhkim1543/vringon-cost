# 원가 QA 검증 보고서 (2026-08-24)

질문: 실제 costing 이 잘 되는가, 전부 연결되어 있는가, 실측과 대조하면 어디서
차이가 나는가. 방법: ① 계산 무결성 감사(결과 파일을 신뢰하지 않고 전부 재계산)
② 신규 이미지 2종 실제 파이프라인 QA ③ 공개된 실측 원가와 대조.

## 1. 계산 무결성 감사 결과

`tools/audit_costing.py` 가 cost.json 을 신뢰하지 않고 독립 재계산한다.
검사 6종 x 프로젝트별: 라인 산술(cost=qty x price), 소요량 재현(소재 스펙
계수로 처음부터), 합계 무결성(소계=라인합, 버킷분해합=소계), 단가 원본
대조(워크북 분기 스냅샷과 값 일치, proxy 는 비율 검증), 게이트 논리(차단
라인은 cost 없음, PARTIAL 이면 총액·FOB 없음), 완제품 질량 재현.

결과: **6개 프로젝트(라인 148개) 전부, 문제 0건.** 산술과 연결은 정확하다.

여섯 디자인의 소계가 $1.99~3.68 대역에 들고, 무거운 디자인(니트·매시3)이
높고 가벼운 디자인(포인트 338g)이 낮게 나오는 상대 순서도 물리적으로 일관된다.

## 2. 실측 벤치마크

| 출처 | 대상 | 수치 |
|---|---|---|
| Wade Motawi (Shoemakers Academy, 전직 신발 개발자 실제 코스트시트) | $70 소매 가죽 스니커 | FOB $15.00 = 어퍼 34% + 가죽 16% + 아웃솔 14% (재료 합 64% ≈ $9.60) + 노무·간접·공장이익 27% + 포장 6% + 금형상각 3% |
| Matthew Kish (Portland Business Journal) | $100 소매 나이키 | FOB $22~25, 운임·보험 $1.00, 관세 $2.50, landed $28.50 |
| 1995 인도네시아 공장 실측 (나이키) | 러닝화 | 재료 $9.00, 노무 $2.75 |
| 업계 통칭 | 러닝화 | 재료 = FOB 의 40~60% |

러닝화급 **재료비 벤치마크: 켤레당 $9~13** (부품 층위 구매가 기준).

## 3. 데모 결과와의 대조

여섯 이미지 전부 실제 생성 엔진으로 만든 결과다 (감사 통과 후 수치).

| 프로젝트 | 어퍼 | 솔 | 화학 | 부자재 | 포장 | 소계 P50 | 완제품 질량 | 판정 |
|---|--:|--:|--:|--:|--:|--:|--:|---|
| 메시 러너(기하) | 0.82 | 1.32 | 0.08 | 0.30 | 1.16 | **3.68** | 673g | 질량 과대 경고 |
| 메시 러너(사내모델) | 0.42 | 0.04 | 0.07 | 0.30 | 1.16 | **1.99** | 129g | 솔 부피 차단(정직) |
| 니트 러너 | 0.97 | 0.92 | 0.08 | 0.30 | 1.16 | **3.42** | 589g | 부피 경고 1건 |
| 가죽 메시 | 0.68 | 0.97 | 0.08 | 0.30 | 1.16 | **3.19** | 474g | 부피 경고 1건 |
| 매시3 (QA 신규) | 1.11 | 0.81 | 0.07 | 0.30 | 1.16 | **3.45** | 552g | 부피 경고 1건 (미드솔 2.0배) |
| 포인트 (QA 신규) | 1.37 | 0.23 | 0.01 | 0.30 | 1.16 | **3.07** | 333g | 폭 too_wide + 부피 경고 |

포인트는 같은 이미지로 두 번 생성됐다(첫 작업의 연결 리셋 복구 과정에서 중복
실행). 두 결과의 소계가 $2.63 과 $3.07 로 갈렸는데, 원인은 생성 3D 의
비결정성이다. 같은 이미지라도 생성마다 세그멘테이션 경계와 부피가 달라져
소계가 약 15% 흔들린다. 이 재현 편차 자체가 C1 불확실성의 실측치이며,
P10~P90 대역($1.3~4.6)이 이를 덮는다.

소재+포장 소계 $3.2~3.7 은 벤치마크 재료비 $9~13 의 **약 30~35%** 다.

## 4. 격차는 어디서 오는가 (정량 분해)

격차 약 $6~9 의 구성:

1. **성형 부품이 원료 kg 단가로만 계상 — 최대 요인 ($2.5~4.5)**
   미드솔 = EVA 컴파운드 $2.165/kg x 질량. 실제 구매는 성형·트리밍된
   부품 단가(미드솔 유닛 $1.5~2.5, 아웃솔 유닛 $1~2)다. 성형 사이클·노무·
   에너지·불량·금형이 빠진 것인데, 이 항목들은 공장 데이터라서 이 데모의
   설계상 **Blocked 로 정직하게 표시**된다 (0 으로 숨기지 않음).

2. **어퍼가 공개 리스팅 하한 단가 ($2.5~4)**
   폴리 니트 메시 $0.8/선형m (1.5m 폭 = $0.53/m²). engineered mesh 실구매
   SKU 는 $3~5/야드급. 워크북 자체가 이 값들을 "Concept only / 신뢰도 C"
   로 명시하고 있고 화면에도 그렇게 표시된다.

3. **소물 일부 미계상 ($0.5~1)**
   봉제사(봉제선 proxy 하한), 아일릿, 삭라이너 폼 일부가 규칙에서 빠지거나
   proxy 하한으로 잡힘.

**민감도 검증**: 수량은 그대로 두고 단가만 부품 층위로 바꾸면
(미드솔 유닛 $2.0, 아웃솔 $1.5, 인서트 $0.6, 어퍼 원단 x4)

| 프로젝트 | 현재 | 부품 단가 가정 |
|---|--:|--:|
| 메시 러너 | $3.68 | **$8.96** |
| 니트 러너 | $3.42 | **$7.45** |
| 가죽 메시 | $3.19 | **$8.43** |

벤치마크 하단($9)에 닿는다. 즉 **격차의 주범은 수량 계산이 아니라 단가
층위(원료 vs 부품)** 이고, 이것은 C1 컨셉 등급의 정의된 한계다. C2(승인
Supplier SKU 견적) 로 가면 벤치마크 범위에 든다.

## 5. QA 중 발견해 수정한 결함 2건

1. **파트 부피 자릿수 게이트 부재.** 니트 러너의 미드솔이 802 cm³/짝
   (통상 100~350 의 2.3배)인데 기존 게이트 셋(복셀 CV 2.5%로 안정,
   폭/길이 0.399 정상, 총질량 589g 정상)이 전부 통과했다. 안정성은
   정확성이 아니고, 총질량은 상쇄될 수 있다. → 성형 파트별 통상 부피
   범위 게이트 신설. 6종에서 경고 3건 검출 (메시러너 1.1배, 니트 2.3배,
   가죽메시 인서트 2.0배). 화면 근거 패널과 원가 카드에 표시.

2. **일시 연결 리셋에 재시도 없음.** 폴링 중 원격 리셋(10054) 한 번에
   몇 분짜리 생성 작업이 통째로 죽었다. → 연결 오류·타임아웃·5xx 에
   지수 백오프 재시도(최대 4회), 서명 URL 다운로드에도 적용. 이미 과금된
   task id 에서 이어가는 복구 스크립트(`tools/resume_example.py`) 추가.

## 6. 결론

- **연결·산술: 정상.** 워크북 단가 → 소요량 → 라인 → 소계 → 게이트가
  전부 재계산으로 검증됐고 0건 불일치.
- **절대값: C1 컨셉 하한이 맞고, 그 이유가 전부 화면에 표시된다.**
  실측 재료비의 30~35% 수준이며 격차의 근원(원료 단가 층위, 성형 전환비
  Blocked, 리스팅 하한)이 각 라인의 근거·자격·차단 사유로 추적된다.
- 실측과 맞추려면 코드가 아니라 **데이터**가 필요하다: 성형 부품 SKU
  견적과 공장 LOP. 그것이 이 시스템의 C2 게이트가 요구하는 바로 그 입력이다.

---

# 추가 검증 (2026-08-24, 2차)

질문 둘. ① 주문 수량·공급사 마진을 바꿀 때마다 값이 바뀌는 것이 맞는가.
② 실제 원가가 공개된 신발과 비교하면 차이가 어디서 오는가.

## 7. 수량·마진 민감도: 바뀌지 않는 것이 맞고, 실제로 바뀌지 않는다

수량 1,000에서 100,000 (100배), 마진 5%에서 40% (8배)로 바꾸며 라이브
서버에서 네 번 계산했다. 소계는 **$3.675640 으로 소수점 6자리까지 동일**했다.
결과 JSON 전체를 재귀 비교했을 때 달라진 필드는 시나리오 입력값 두 개뿐,
계산된 값은 단 하나도 바뀌지 않았다.

이것이 옳은 동작이다.

- 소재비는 켤레당 계산이다. 수량이 늘어도 켤레당 소재량은 그대로다.
  수량이 원가를 낮추려면 **수량 구간별 단가표(MOQ 브레이크)** 가 있어야 하는데
  워크북에는 분기 스냅샷 단가만 있고 구간 단가가 없다. 없는 할인을 지어내지 않는다.
- 주문 수량이 실제로 쓰이는 곳은 **금형 상각**(금형비 ÷ 수량)인데, 금형 견적이
  TBD 라 차단 상태다. 분모만 있고 분자가 없으니 결과가 없다.
- 공급사 마진이 쓰이는 곳은 **FOB**(제조원가 × (1+마진))인데, 제조원가가
  부분 원가라 차단이다. 소재비에 마진을 곱해 FOB 를 만들지 않는다.

즉 두 입력이 지금 아무 일도 하지 않는 것은 결함이 아니라 차단 설계의 결과다.
다만 화면이 그것을 말해주지 않아 사용자가 오해할 수 있었으므로, 부분 원가일 때
"지금은 이 두 값이 결과를 바꾸지 않습니다"와 그 이유를 원가 화면에 표시하도록
고쳤다.

## 8. 실제 공개 원가와의 대조

| 출처 | 대상 | 소재비/켤레 | 성격 |
|---|---|--:|---|
| Shoemakers Academy 코스트시트 | $70 소매 스니커, FOB $15.00 | **$9.60** (어퍼 34% $5.10 + 가죽 16% $2.40 + 아웃솔 14% $2.10) | 전직 신발 개발자의 실제 코스트시트 |
| Everlane (투명 가격제) | Tread 스니커, 소매 $98 | **$22.04** (부자재 $0.60, 노무 $9.15, 관세 $3.18, 운송 $1.50, 합 $36.47) | 브랜드가 디자인별로 공개한 실제 원가 |
| Portland Business Journal (Kish) | $100 소매 나이키 | FOB $22~25, landed $28.50 | 기자 취재 |

Everlane Tread 는 가죽·청키 솔의 라이프스타일 스니커라 소재비 상단이고,
$70 스니커 코스트시트가 우리 데모(메시 러너)에 가장 가깝다.

## 9. 차이를 항목별로 이은 다리

우리 값은 **원료 kg 단가** 기준이고 공개 코스트시트는 **부품 구매가** 기준이다.
그냥 빼면 "얼마나 틀렸나"만 남으므로, 조정 항목을 하나씩 쌓아 다리를 놓았다
(`tools/benchmark_bridge.py`, 재현 가능).

| 단계 | 메시 러너 | 근거 |
|---|--:|---|
| 우리 계산 (C1, 원료 단가) | 3.68 | 감사 통과 값 |
| 성형 부품을 부품 유닛 단가로 | +2.83 | 미드솔 유닛 $1.5~2.5, 아웃솔 $1~2, 인서트 $0.4~0.8 |
| 어퍼 원단을 실구매 단가로 | +1.41 | engineered mesh 야드당 $3~5 대 워크북 $0.80/m |
| 빠진 필수 부자재 7건 | +1.35 | 깔창 $0.60, 칼라 라이닝, 아일렛, 하드너, 라벨, 인솔보드, 카톤 |
| 신발 상자를 벌크 단가로 | −0.65 | 우리 $1.15 는 과대. 기준의 포장 **전체**가 $0.90 |
| **조정 후** | **8.62** | |
| 기준 실측 소재비 | 9.60 | |
| 남은 차이 | −0.98 (−10%) | 조정값 자체의 불확실성 안 |

니트 러너 $6.95(−28%), 가죽 메시 $8.11(−16%) 로 같은 다리에서 재현된다.

### 이 다리가 말해주는 것

1. **수량 계산은 맞다.** 어퍼 외피 순면적 0.070 m²/짝(켤레 0.14 m²)은 러닝화
   통상 범위 안이고, 미드솔 질량 97 g/짝은 실제 EVA 미드솔 100~140 g 범위에
   가깝다. 물리량이 틀렸다면 어떤 단가를 넣어도 기준에 수렴하지 않는다.
2. **차이의 주범은 단가 층위다** (+2.83, +1.41 = 전체 조정의 76%). 성형·트리밍·
   불량·금형이 들어간 부품 단가 대신 원료 단가를 쓴 결과이고, 그 전환비는
   공장 데이터라 이 데모가 **의도적으로 차단**하는 항목이다.
3. **BOM 누락이 실재한다** (+1.35). 규칙 52개 중 이 디자인에서 라인이 나온 것은
   23개, P0(필수) 중 10개가 안 나왔다. 그중 깔창(Sockliner Foam·Cover),
   칼라 라이닝, 아일렛, 하드너는 **모든 러닝화에 있는 부품**이라 진짜 누락이다.
   나머지(쿼터·아이스테이·머드가드·텅 쉘)는 세그멘테이션이 어퍼를 한 덩어리로
   잡아 Vamp 면적에 흡수된 것이라 이중 누락은 아니다.
4. **과대 계상도 하나 있다.** 신발 상자 $1.15 는 기준 코스트시트의 포장 전체
   $0.90 보다 크다. 아웃솔 러버 155 g/짝도 실제 60~100 g 대비 무겁다
   (완제품 673 g/켤레, 실제 러닝화 500~600 g — 화면이 이미 질량 과대로 경고).

### 결론

- **엔진은 정상이다.** 산술·연결·게이트는 감사에서 0건 불일치이고, 수량은 물리적
  실측 범위에 든다. 수량·마진이 결과를 안 바꾸는 것도 설계대로다.
- **$3.68 이라는 절대값은 "정상 원가"가 아니라 C1 컨셉 하한이다.** 실제 소재비의
  약 38%이며, 부족분의 76%는 이 데모가 일부러 막아 둔 공장 데이터(성형 전환비)에서
  온다. 부품 층위 단가를 넣으면 $8.62 로 기준 $9.60 의 90%에 닿는다.
- **당장 고칠 수 있는 것은 BOM 누락 4건**(깔창·칼라 라이닝·아일렛·하드너)이다.
  이것은 데이터가 아니라 규칙 문제라 코드로 해결된다.

---

# 결함 전수 수정 (2026-08-24, 3차)

"고칠 수 있는 건 모두 고쳐라"에 따라 BOM 생성 경로를 전수 점검했다. 결함 6건을
찾아 고쳤고, 남은 것은 코드가 아니라 데이터가 필요한 항목이다.

## 10. 고친 결함

### D1. 워크북 규칙의 기하 파라미터가 통째로 무시되고 있었다 (가장 큰 결함)

`construction_recipes` 의 Default Parameter 열(coverage·ratio·allowance)은
"측정 면적 중 이 파트가 실제로 덮는 비율"인데, 소요량 계산이 이 값을 전혀 읽지
않았다. 라인에 실려 화면에 보이기만 하고 곱해지지는 않았다.

결과적으로 토퍼프는 앞코 **전체**를(실제 0.65), 힐카운터는 뒤꿈치 전체를(0.80),
안감은 어퍼 전체를(0.85) 덮는 것으로 계산돼 **과대계상**이었다. 반대로 아일렛
보강은 1.15 를 못 받아 과소계상이었다.

`bom.apply_rule_scale()` 로 반영했다. pattern·yield 는 일부러 제외했다 — 그 둘은
소재 스펙의 pattern_factor·nesting_yield 가 이미 들고 있어 겹쳐 곱하면 이중
적용이 된다. 적용된 배율은 근거 패널에 "규칙 R-006 배율 coverage=0.65" 로 남는다.

### D2. 3D 에 안 보이는 필수 파트가 BOM 에서 통째로 빠져 있었다

BOM 마스터 52개 파트 중 라인이 나온 것은 23개였다. 세그멘테이션은 **외형**만
잡고 워크북 규칙표는 21개 규칙뿐이라, 깔창·봉제사처럼 **모든 신발에 있지만
겉에서 안 보이는** 파트는 어느 경로로도 들어오지 못했다.

`construction_recipes_supplement.json` 에 보완 규칙을 넣었다(깔창 폼, 깔창 표피,
봉제사, 금속 아일렛). 워크북 출처가 섞이지 않게 **파일을 분리**했고 근거에
"워크북 규칙표 외 보완"으로 표시된다. 금속 아일렛은 메시 러닝화에 보통 없으므로
플래그 기본값을 꺼 두었다.

### D3. 칼라 안감을 넣으려다 이중계상을 발견

처음에는 칼라 안감도 추가했다. 그런데 워크북 R-002(Vamp/Quarter Lining)의 측정
기준인 `upper_proxy_area` 에 Collar Shell 이 **포함**돼 있어, 칼라 안감을 따로
더하면 같은 면적을 두 번 사는 것이었다. 규칙을 되돌리고 회귀 테스트로 고정했다.
R-002 의 ratio=0.85 가 칼라를 제외한 값인지는 워크북 소유자 확인이 필요하다.

### D4. 하드너가 존재하지 않는 것으로 처리됐다

`adhesive_requires_hardener` 플래그가 False 라 워크북 규칙 R-015 가 아예 안 돌았다.
신발용 PU 접착제는 통상 2액형이라 하드너는 실재한다. 플래그를 켜고, 워크북이
산식으로 지정한 `adhesive_mass` 를 구현했다(하드너는 접착제에 섞여 같은 접착
면적에 발리므로 도징 비율은 소재 스펙이 든다). 이제 **수량은 1.05 g/켤레로
계산되고 단가만 RFQ 로 차단**된다. 0 으로 숨기지 않는다.

### D5. 차단 사유가 진짜 원인을 감췄다

산식이 미구현이어서 막힌 것을 "도포 면적 없음"으로 표시해, 세그먼트가 없는
것인지 산식이 없는 것인지 구분할 수 없었다. 이제 "산식 'adhesive_mass' 미구현"
처럼 원인을 그대로 말한다.

### D6. 파트 두께가 두 곳에 다르게 적혀 한쪽이 무시됐다

규칙 R-005 는 칼라 폼을 6mm 로 지정하는데 소재 스펙의 대표값은 4mm 였고, 질량
계산은 스펙 값만 썼다. 규칙이 파트별로 지정한 두께가 이 디자인의 실제 값이므로
그 비율로 면밀도를 보정한다. 감사기도 같은 규칙으로 독립 재현하도록 고쳤다.

**재발 방지**: 규칙에 숫자를 적어 뒀는데 계산 어디에서도 쓰이지 않으면
`rule_params_ignored` 에 남긴다. 지금은 전 프로젝트에서 0건이다. D1 은 이 장치가
없어서 오래 숨어 있었다.

## 11. 수정 후 값

| 프로젝트 | 이전 | 이후 | 라인 | 질량 판정 |
|---|--:|--:|---|---|
| 메시 러너 | 3.676 | **3.822** | 25/28 | 688g 과대 (솔 부피 과대) |
| 사내모델 | 1.988 | **2.117** | 22/26 | 142g BOM 누락 의심 (솔 부피 차단) |
| 니트 러너 | 3.419 | **3.573** | 24/27 | 604g ok |
| 가죽 메시 | 3.192 | **3.262** | 26/29 | 484g ok |
| 매시3 | 3.453 | **3.548** | 25/29 | 557g ok |
| 포인트 | 3.069 | **3.058** | 25/28 | 314g 확인 필요 |
| 라이브 | 3.221 | **3.274** | 26/30 | 492g ok |

깔창·봉제사·하드너가 들어오고(+), 과대계상이던 토퍼프·힐카운터·안감이 줄어(−)
순증은 작다. 중요한 것은 액수가 아니라 **이제 각 항목이 실제 규칙대로 계산된다**는
점이다. 감사 0건, 회귀 51건 통과.

## 12. 아직 데이터가 필요한 것 (코드로 못 고침)

| 필요한 데이터 | 막고 있는 것 | 지금 화면 표시 |
|---|---|---|
| **성형 부품 SKU 견적** (미드솔·아웃솔 유닛 단가) | 소재비의 최대 격차 요인 ($2.8) | 원료 kg 단가로 계산, C1 한정 |
| **공장 Routing·SAM, Loaded Labor Rate, Machine Rate** | 전체 제조원가·FOB 전부 | 차단 (부분 원가) |
| **금형 견적** | 금형 상각, 주문 수량 반영 | 차단 |
| **하드너 단가** (워크북 RFQ) | 하드너 라인 | 수량은 계산, 단가 차단 |
| **아일렛·라벨·마스터카톤 소재 스펙** | 소물 3건 | 라인 없음 |
| **승인 sole CAD** | 미드솔·아웃솔 부피 과대 (메시러너 389cm³, 통상 상한 350) | 부피 경고 + C1 상한 |
| **승인 pattern/DXF** | 봉제선 길이가 하한 proxy | 근거에 proxy 표시 |
| **신발 상자 실구매 단가** | 워크북 범위가 $0.3~2.0 로 너무 넓다. 현재 중앙값 $1.15 는 기준 코스트시트의 포장 **전체** $0.90 보다 크다 | 워크북 값 그대로 |
| **R-002 ratio=0.85 의 정의** (칼라 포함 여부) | 칼라 안감 라인 가부 | 이중계상 우려로 제외 |
| **어퍼 원단 실구매 단가** | engineered mesh 실구매는 야드당 $3~5, 워크북은 $0.80/m 리스팅 하한 | 리스팅 하한, 신뢰 C |

---

# 외부 감사 대응 (2026-08-24, 4차)

외부 검토에서 P0 5건, P1 다수를 지적받았다. 코드로 확인 가능한 것은 전부
재현해 보고 고쳤다. 지적 중 사실이 아닌 것은 없었다.

## 13. P0 — 고침

| 지적 | 재현 결과 | 조치 |
|---|---|---|
| WebGL 실패가 앱 전체를 중단 | 사실. `new Viewer()` 가 모듈 최상위라 던지면 모듈이 통째로 죽어 BOM·단가·원가까지 못 봄 | 생성 실패를 격리하고 아무 일도 하지 않는 대체 뷰어로 계속 진행. 뷰어 자리에 안내를 띄우고 나머지 화면은 그대로 동작. `?no3d=1` 로 그 상태를 재현할 수 있게 함 |
| 소재가 미가격이어도 COMPLETE 가능 | 사실. Material 버킷은 라인이 막혀도 나머지 합으로 p50 이 채워져 완성 판정에서 빠지지 않음 | 필수 버킷은 금액뿐 아니라 **상태까지** 확인. 지금 메시 러너의 차단 버킷에 Material 이 정직하게 들어감 |
| Machine 원가가 조용히 0 | 사실. 기계 시간·rate 가 없으면 `None` 으로 건너뛰고 차단 목록에 들어가지 않음 | 수작업 workcenter(검사·포장)만 0 으로 인정하고, 나머지는 데이터가 없으면 차단 |
| 게이트를 증거 없이 변경 가능 | 사실. 참/거짓만 받으면 승인 기록이 아니라 플래그 | 승인자와 근거를 필수로 요구하고 변경 이력(`gate_log`)을 남김. 알 수 없는 게이트 이름은 거부 |
| 서버 보안 | 사실. 프로젝트 ID 무검증(경로 탈출), 업로드 크기·형식 무검증, traceback 노출 | ID 화이트리스트 검증, 업로드 20MB 상한과 확장자·매직바이트 확인, traceback 은 로컬에서만. 조회만으로 저장소가 생기던 것도 막음 |

라이브에서 실제로 확인한 결과: 경로 탈출 차단, 가짜 확장자 거부, 게이트
무근거 변경 400, 운영 응답에 traceback 없음.

## 14. P1 — 고침

- **백분위 표기가 과장이었다.** 라인별 단가 범위를 단순 합산한 값에 P10/P50/P90
  이라는 이름을 붙이면 분포를 모델링한 것처럼 보인다. 화면 표기를
  **낮음·기준·높음**으로 바꾸고, 상관을 반영하지 않아 합계 구간이 실제보다
  넓을 수 있다고 명시했다.
- **접근성**: 표 행을 키보드로 선택 가능하게(`tabIndex`, Enter/Space),
  안내 메시지에 `role="status" aria-live`, 초점 표시 추가.
- **비동기 경합**: 예시 목록이 늦게 도착했는데 사용자가 이미 다른 단계로
  넘어갔으면 대상 요소가 없어 콘솔 오류가 났다. 매번 다시 찾고 없으면 그만둔다.
- **실서버에서 나던 404**: 정적 모드 판정을 위해 `data/index.json` 을 찔러
  보느라 실서버에서는 늘 404 가 찍혔다. 정적 빌드가 표시를 남기고 그때만 읽는다.

## 15. 지적 중 이미 알고 있었고 데이터가 필요한 것

발주수량이 단가 tier·MOQ 에 반영되지 않는 점, 원재료와 구매 완제품 부품의
경계, 재현성 15% 편차, 단일 construction recipe, reject·overhead 의 일괄 비율
적용은 모두 §12 의 "데이터가 필요한 것" 목록과 같은 뿌리다. 코드로 메울 수
없으므로 §12 를 그대로 둔다.

## 16. 포지셔닝

외부 검토의 표현이 정확하다. 이 도구는 **"AI 신발 원가 계산기"가 아니라
C0~C1 단계의 디자인 기반 Should-Cost 탐색기**다. 화면의 금액은 완성 신발
원가가 아니라 공개 단가와 3D 추정량으로 계산한 **일부 재료·포장비 소계**이며,
그 사실이 화면·문서·이 보고서에 일관되게 표시된다.

---

# 외부 개선안(PDF) 반영 (2026-08-28, 5차)

`VRINGON_신발_Costing_구조_AI모델_개선안.pdf` 의 지적 12건을 코드로 하나씩
검증했다. **10건 CONFIRMED, 2건 PARTIALLY_TRUE, 사실이 아닌 것은 없었다.**
8건은 코드만으로 고칠 수 있어 이번에 반영했고, 4건은 공장·공급사 데이터가
있어야 한다.

## 17. PDF 가 센 수치를 그대로 재현했다

- **가정 파라미터 66.4%** — 우리 집계 77/116 = 66.4%. PDF 는 71/107 로 셌는데
  분모가 다른 것은 그 뒤 하드너 스펙이 추가됐기 때문이고 **비율은 소수점까지
  일치**한다.
- **승인 견적 단가 0/34** — 사실. 34개 분기 단가가 전부 공개 listing 이다.
- **가죽 오분류** — 사실이고 원인까지 확인했다. `part_defaults` 가 canonical
  part 하나에 소재 하나를 고정해, "가죽 메시" 데모에 가죽 소재(MAT-FULLGRAIN)가
  **한 번도 쓰이지 않았다.** 갑피가 전부 메시 단가($0.80/m)로 계산됐다.

## 18. 고친 것

### P1. 미승인 라인이 확인된 소계에 섞여 있었다 (가장 큰 결함)

`approval_status` 는 만들어지기만 하고 합계·차단·등급 어디에서도 읽히지
않았다. 실측하니 규칙이 제안한 숨은 BOM 이 **소계의 56~61%** 를 차지하면서
아무 표시 없이 '확인된 소계' 에 들어가 있었다.

이제 승인분과 미승인분을 나눈다. 금액을 지우는 것이 아니라 **어디에 합산되는지**를
바꾼다. 미승인이 있으면 Material 버킷이 차단되어 FOB 가 나가지 않고, 게이트만
켜서 C2 로 올라가는 경로도 막힌다. 승인은 `POST /project/{id}/bom/approve` 로
승인자와 근거를 남기고 한다.

| 프로젝트 | 확인된 소계(승인) | 미승인 | 승인 라인 |
|---|--:|--:|---|
| 메시 러너 | 1.797 | 2.026 | 9/28 |
| 니트 러너 | 1.409 | 2.164 | 8/27 |
| 매시3 | 1.365 | 2.183 | 11/29 |
| 포인트 | 1.033 | 2.025 | 9/28 |
| 사내모델 | 0.494 | 1.623 | 8/26 |

### P2. 소재가 canonical part 별로 고정이었다

파트 또는 세그먼트별로 소재를 고를 수 있게 했다(`/materials`). 고르지 않으면
기본값을 쓰되 **"승인 안 됨"** 으로 표시된다. 가죽 데모에 가죽을 지정하니
갑피가 $0.029 → $3.541 로 바뀌었다. 전체는 $3.26 → $8.67 이고, 벤치마크
다리에서 $11.17(기준 $9.60 대비 +16%)로 **위쪽**에 선다. 가죽 스니커라면
타당한 방향이다.

가죽 공학 스펙도 새로 넣었다. 가죽은 외곽이 불규칙하고 결점을 피해 재단해야
해서 **nesting yield 0.62** 로 롤 원단(0.82~0.85)보다 낮다.

### P3. 단위가 달라도 차원만 같으면 통과했다 (잠복 결함)

`check_multiply` 가 `count` 계열만 정확 일치를 요구하고 면적·길이·질량은
차원만 봤다. 가죽이 **sq ft** 로 팔리므로 m² 수량에 USD/sq ft 를 곱하면
**10.76배** 틀린 값이 조용히 나올 자리였다. 이제 환산 계수를 돌려주고,
환산이 일어나면 계산 근거에 "단가 환산 sq ft → m² 계수 10.7639" 로 남긴다.
PDF 의 price master 필수 필드 "UOM & conversion" 에 해당한다.

### P4. 3D 표면적을 measured 로 불렀다

갑피 면적은 곡면을 편 것도 아니고 시접·마커도 없어 재단 패턴 면적이 아니다.
`proxy` 로 낮추고 "승인 DXF/marker 로 대체 필요" 를 근거에 붙였다.

### P5. 이 원가가 무엇에 기대는지 스스로 말한다

PDF 가 직접 세어서 지적한 숫자는 결과물이 스스로 말해야 한다. 원가 화면에
증거 커버리지를 넣었다: 승인 공급사 견적 비율(현재 0%), 엔지니어 승인 라인
비율, 실측 대 대체값 지오메트리 건수, 가정 파라미터 비율(현재 72%).

### P6. 원가 버전 키

style·colorway·size range·factory·country·수량·분기·통화·supplier·quote
version·Incoterm 을 결과에 stamp 하고, **선언되지 않은 항목을 함께 남긴다.**
현재 7항목이 미선언이라 `comparable=false` 다. 같은 조건에서 비교했다고
말할 수 없다는 뜻이다.

## 19. 아직 데이터가 필요한 것 (§12 와 같은 뿌리)

| PDF 요구 | 왜 코드로 안 되는가 |
|---|---|
| 승인 supplier quote·PO·invoice price master | 실거래가가 없다. 스키마는 준비됐고 값이 없다 |
| DXF pattern nesting·size run | 승인 패턴이 없으면 3D 표면적 proxy 가 최선이다 |
| sole CAD 또는 component quote | 성형 부품 단가가 없어 원료 kg 단가로 남는다 |
| routing·SAM·machine·tooling | 15개 공정 전부 TBD. 그래서 FOB 가 차단이다 |
| driver-based overhead·rework | 적용 기준이 공장마다 다르다. 지금은 flat percent 이고 그렇다고 표시한다 |
| actual-vs-estimate gold dataset | 실제 cost sheet 가 없으면 MAPE 를 잴 수 없다 |

## 20. 포지셔닝 (PDF 권고 그대로)

**AI 가 제안하고, 공학 데이터가 계산하며, 공장이 검증한다.** 현재 판매 가능한
포지셔닝은 "이미지 기반 C1 concept should-cost 와 cost-driver 탐색" 이다.
C2 는 승인 BOM·DXF/CAD·supplier quote·routing·tooling 이 모두 연결된
스타일에만 열린다.

---

# 2차 외부 검토 반영 (2026-08-28, 6차)

AI 모델 구조까지 다룬 2차 검토를 반영했다. 핵심 명제는 "가격을 맞히는 거대
모델이 아니라, 제안·검증·계산·승인을 분리한 여러 개의 작은 전문 모델"이고,
이 데모의 설계 방향과 같다. 코드로 가능한 것을 반영하고, 나머지는 데이터
로드맵으로 남긴다.

## 21. 검토가 지적한 오류 2건 (둘 다 사실, 수정)

1. **material_approved 전파 누락.** bom 이 소재 승인 플래그를 만들었는데
   costing 이 원가 라인으로 복사하지 않아, CSV 의 '소재승인됨' 이 **항상
   '아니오'** 였다. 가죽을 승인 지정한 라인조차 미승인으로 보였다.
2. **변경 무효화 부재.** 수량·분기·소재·게이트를 바꿔도 기존 cost.json 이
   아무 표시 없이 그대로 보였다. 실측: 수량을 5000→99999 로 바꿔도 화면은
   5000 기준 결과를 계속 보여줬다. 이제 계산 시점의 **입력 지문**(시나리오·
   소재 선택·게이트·매핑·복구·BOM 승인, 각각 해시)을 결과에 남기고, 읽을 때
   현재 상태와 비교해 "이 결과는 낡았습니다. 바뀐 것: 시나리오" 를 표시한다.

## 22. 등급 사다리 C0~C4 재정의

검토안의 사다리를 그대로 채택했다. routing·tooling 은 소재 원가의 조건이
아니라 **공장 제조원가(C3)** 의 조건이므로 C2 에서 C3 로 옮겼다.

| 등급 | 이름 | 필수 근거 |
|---|---|---|
| C0 | Visual Concept | 단일 이미지 (BOM·소재 후보만) |
| C1 | Concept should-cost | scale 확정 + 기준 단가 |
| C2 | Material should-cost | 승인 패턴/CAD + 승인 소재 SKU 견적 |
| C3 | Factory manufacturing cost | routing·SMV·rate·tooling·간접비 정책 |
| C4 | EXW/FOB | Incoterm 선언 + PO·invoice·실적 대사 |

등급 표시에 이름이 붙고, 미충족 사유가 등급별로 나온다. 데모 7종은 전부
C1 이 맞다. 검토의 결론대로 **C3 자료 없이 임의 상수로 총액을 만드는 것은
사실성을 떨어뜨리므로 하지 않는다.**

## 23. 가격 신뢰도 계층 A0~A4

| 계층 | 뜻 | 현재 보유 |
|---|---|--:|
| A4 Actual | 송장·입고·실제 결제 | 0 |
| A3 Committed | 승인 PO·계약단가 | 0 |
| A2 Quoted | 유효한 공급사 견적 | 0 |
| A1 Benchmark | 원자재·시장 지수 | 7 (고무 4종·EVA·LDPE·판지) |
| A0 Estimated | 공개 리스팅·카탈로그 | 27 |

라인마다 계층이 붙고, 증거 패널이 "가격 신뢰도 계층: Estimated 22건 ·
Benchmark 4건 (Actual 과 Quoted 가 없으면 전부 추정 기반입니다)" 로 집계한다.
공개 시세를 정답이 아닌 benchmark 로 격하하라는 검토 원칙 그대로다.

## 24. 소요량 산출방법 명시

라인마다 `consumption_method` 가 붙는다. 현재는 전부 3D proxy 계열
(`surface_proxy_3d`, `volume_proxy_3d`, `bond_area_proxy_3d`,
`seam_length_proxy_3d`, `fixed_count`, `fixed_mass`)이다. 생산용 방법
(`dxf_marker`, `leather_hide_nesting`, `cad_volume`, `supplier_unit_quote`)은
이름만 예약해 두고, 해당 데이터가 없는 지금은 **절대 나타나지 않는 것**을
테스트로 고정했다. 3D 표면적에 일률 waste 를 더하는 방식과 nesting 을 겹치면
이중계산이 된다는 경고도 이 분리로 지킬 수 있다.

## 25. 코드로 안 되는 것 (검토안 로드맵 순서대로)

1. DXF/marker 기반 consumption 엔진 — 승인 패턴·grading·nesting 데이터 필요
2. supplier quote·PO·invoice Price Fact — 실거래 문서 필요 (A2~A4 가 비어 있다)
3. routing·SMV 엔진 — 공정별 표준시간과 공장 rate 필요
4. leather hide scan·품질구역 nesting — hide 데이터 필요
5. multiview 3D·소재 retrieval 파인튜닝 — 신발 정답 데이터셋 필요
6. actual 대비 residual calibration — 실제 cost sheet 필요

검증 67건, 감사 0건.
