# 내 디자인으로 직접 돌려보기

공개 데모(https://jhkim1543.github.io/vringon-cost/)는 미리 계산된 결과를 보는
읽기 전용입니다. 3D 생성은 백엔드가 필요하므로 로컬에서 실행해야 합니다.

## 1. 준비

```bash
run.cmd
```

브라우저에서 http://127.0.0.1:5270 을 엽니다.
생성 API 키는 환경변수 `MESH_API_KEY` 또는 세션 루트의
`..\scripts\run_backend.cmd` 에서 읽습니다. 리포에는 키를 두지 않습니다.

## 2. 이미지 준비

가장 잘 나오는 조건입니다.

* 배경이 흰색이거나 단색
* 신발 전체가 잘리지 않고 들어갈 것
* 정측면(lateral) 한 장이 최소 조건
* 그림자와 반사가 적을 것

권장 해상도는 긴 변 1024px 이상입니다.

## 3. 실행

화면은 디자인 단계에서 시작합니다. 두 가지 방법이 있습니다.

예시로 시작: 예시 디자인 카드(메시 러너, 니트 러너, 가죽 메시)를 클릭하면
미리 계산된 결과가 열립니다. 이후 각 단계 하단의 확정 버튼으로 넘어갑니다.

내 이미지로 시작:
1. 점선 업로드 존에 이미지를 끌어다 놓거나 클릭해 선택
2. 미리보기와 해상도 확인
3. `3D 생성과 세그멘테이션 실행` 클릭

생성 30 크레딧, 세그멘테이션 40 크레딧이 소모되고 3에서 6분 걸립니다.
완료되면 새 프로젝트로 자동 이동합니다.

## 4. 이후 단계

1. `스케일` 실제 외부 outsole 앞코 뒤꿈치 길이를 mm 로 입력하고 확정합니다.
   사이즈 260 은 외부 길이가 아닙니다. 브랜드와 모델마다 다르므로 실측값이
   필요합니다. 3D 에서 앞코와 뒤꿈치 점을 직접 옮길 수 있습니다.
2. `세그먼트` 자동 배정을 확인하고 필요하면 드롭다운으로 고칩니다.
   `부피 막힌 파트 복구` 를 누르면 솔 부피를 복구합니다.
3. `BOM` 부터는 자동으로 계산됩니다.

## 5. 형상 정확도에 대한 주의

측면 한 장으로는 반대쪽 quarter, 아웃솔 트레드, 평면 폭, 힐 뒷면이 보이지
않습니다. 치수를 맞춰도 형상 정확도는 별개입니다. 여러 각도의 실제 사진이
있으면 `server/mesh_provider.py` 의 `multiview_to_model` 로 붙일 수 있고,
그쪽이 훨씬 정확합니다.

## 6. 다른 프로젝트 보기

주소 뒤에 `?p=프로젝트ID` 를 붙입니다.
예: http://127.0.0.1:5270/?p=RUN-260824
